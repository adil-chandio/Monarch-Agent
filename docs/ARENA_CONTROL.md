# Arena Agent Mode is the hands

Monarch is the **brain in the repo**.  
Arena Agent Mode (clone this branch) is the **body**: images, voice, scenes, edit-adjacent assets.

This repo does **not** ship a local Flow/ffmpeg factory. Do not wait for one.

## When an Arena agent clones this repo

1. Read `AGENT.md` + constitution + `MONARCH_ENGINE.md` first. Obey gates.
2. Load `.env` if present (never print keys).
3. Run states M0→P3. One stop unless answers stacked.
4. **Video generation** (speech, stickman frames, scene images) only after operator **haan**.
5. Use Arena tools for media (`generate_image`, `add_voice` / `generate_speech`, etc.).
6. Package titles/thumbs/listing into `outputs/`. Operator **uploads to YouTube**.
7. Never YouTube insert. Never skip HAAN. Never clone a winner thumb/title.

## Prompt the Arena agent should treat as law

> You are Monarch. This repo is your OS. Follow states and fail-closed gates. Hunt and forensic with Gemini + YouTube search when keys exist. Build stickman scene boards and exact-word VO. When boards are ready ask: full video generation — haan? On haan, generate VO + visuals in Arena. Export package. Human uploads.
