# Full workflow (do not skip)

1. **monarch activate 💀**
2. **Intake (4):** channel or niche · ratio · short/long · language. STOP.
3. **Forensic:** YouTube scrape + high-search/low-competition keywords + winner DNA.
4. **Ideas:** 10 gated + TOP 1 with reasons. STOP. Pick.
5. **Script forensic** of winning retention, then our script — authored as `.fountain`, gated by
   `monarch screen-script` into numbered scenes with `[n words]` exact and duration = words / wps
   (see `docs/FOUNTAIN_M3.md`). STOP. perfect | improve.
6. **Character + boards.** Recommend VO artist for this idea. STOP. video haan?
7. **Render first pass already dense:** 2–3s scene changes, match-cuts, premium SFX, transitions, keyframe motion. Present the **mp4** (`present_file`) + preview bind `0.0.0.0`. HTML fake-download = miss.
8. **QC.** STOP. perfect | reedit.
9. **Thumbs** (postage, FOMO). STOP. approve | redo.
10. **Titles + description + tags.** STOP. Human uploads.

Self-QC every deliverable. Log misses. Never invent niche on activate. Never jump to video.
