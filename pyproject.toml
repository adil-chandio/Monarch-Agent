[project]
name = "monarch-agent"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]

[project.scripts]
monarch = "monarch.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
