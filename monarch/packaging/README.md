# Packaging pipeline (structure)

```
forensic_ctr.py   # competitor title/thumb DNA — stub, no live scrape
titles.py         # 10 title candidates + kill list
thumbs.py         # 5 composition prompts + CapCut headlines
listing.py        # description, tags, chapters
upload.py         # draft only, gated
```
