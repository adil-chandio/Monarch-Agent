# Activate intake (mandatory)

`monarch activate` ≠ invent Educational Explainer.

First message after activate = four questions only. Wait.

Then scrape. Then keywords (searchable, low competition). Then ideas.

Never default-niche hunt on an empty activate.
