# Upload

The operator uploads. Monarch never calls YouTube upload APIs.

Agent delivers `outputs/*.json`: ranked titles, listing, thumb prompts, scenes, animation prompts.

Privacy default in the package is private so the human can still post as draft.
