# Arena

Renderer = **Arena Agent Mode**, not a Python video backend.

`NotImplementedError` on ffmpeg/Flow is correct. Do not “fix” it by bolting a half factory.

When HAAN fires, the Arena agent generates media **in that session** using this repo’s prompts, locks, maths, and gates.
